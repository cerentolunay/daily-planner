# DailyPlanner

AI destekli günlük planlayıcı için monorepo temel kurulumu.

## Çalıştırma

1. `.env.example` dosyasını kopyala ve `.env` olarak adlandır.
2. `docker compose up --build` komutunu çalıştır.

## Servisler

- Frontend: http://localhost:3000
- Backend: http://localhost:8000
